"""
By default, the heroes module was not downloadable, but PyCharm gave suggestions to install the package
first, and then we can import & use the package as needed.
"""
import heroes

print(heroes.gen())
